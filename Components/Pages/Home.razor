@page "/"
@inject NavigationManager NavigationManager

<PageTitle>Home</PageTitle>

<!-- Título principal -->
<div class="hero-section text-center">
    <h1 style="font-family: 'Poppins', sans-serif; color: #1c3144;">
        ¡Bienvenido a <strong>DriverChoice</strong>!
    </h1>
    <p class="lead" style="font-size: 1.4rem; color: #3f88c5;">Tu plataforma confiable para alquilar vehículos de forma fácil y rápida.</p>

    <!-- Imagen de vehículo o algo relacionado con el alquiler -->
    <div style="width: 100%; max-width: none;">
        <img src="https://urlshort.app/D0QQTA"
             alt="Alquiler de vehículos"
             style="width: 70%; height: auto; border-radius: 5px;">
    </div>

    <!-- Botones CTA -->
    <div class="cta-buttons mt-4">
        <button class="btn btn-primary btn-lg" style="background-color: #ffba08; border-color: #ffba08; font-size: 1.2rem;" @onclick="VerVehiculosDisponibles">
            Ver Vehículos Disponibles
        </button>

        <!-- Enlace al inicio de sesión -->
        <NavLink class="btn btn-secondary btn-lg ms-3" style="background-color: #3f88c5; border-color: #3f88c5; font-size: 1.2rem;" href="/Account/Login">
            Iniciar Sesión
        </NavLink>

        <NavLink class="btn btn-secondary btn-lg ms-3" style="background-color: #3f88c5; border-color: #3f88c5; font-size: 1.2rem;" href="/vehiculos/create">
            Agregar Vehículo
        </NavLink>
    </div>
</div>

@code {
    private void VerVehiculosDisponibles()
    {
        // Navega a la página de catálogo de vehículos
        NavigationManager.NavigateTo("/catalogo");
    }
}